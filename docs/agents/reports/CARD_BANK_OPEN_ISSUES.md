# CARD_BANK_OPEN_ISSUES

- General `words` coverage remains modest compared with professional banks; the obvious PUHIS donor word lists were rejected because they are mixed-language or noisy.
- General `phrases` coverage is present but still smaller than professional sentence coverage.
- Some professional vocab banks likely contain near-duplicates created upstream; the duplicate report lists signature-level collisions only.
- SQL publication lifecycle was not executed here; runtime coverage relies on validated sources plus the generated supplement files.
