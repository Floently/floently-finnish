# Pass 5 Final Open Items

- manual QA is still recommended for the YKI exam overview/admin bank flows and cards adaptive session flow because Pass 5 verified imports, boot, typecheck, lint, and stale-path resolution rather than full end-to-end user sessions
- external deployment/runtime prerequisites remain: the backend still needs the configured external YKI engine endpoint for live YKI exam/session traffic
- no duplicate-source cleanup work remains in the live repo for the scoped cleanup families
