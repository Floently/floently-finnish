import { getExamSession, startExamSession, submitExam, submitExamAnswer } from '@core/api/examRuntime';
export const examRuntimeApi = { getExamSession, startExamSession, submitExam, submitExamAnswer };
