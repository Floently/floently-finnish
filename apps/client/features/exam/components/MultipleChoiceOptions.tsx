import React from 'react'; import { Pressable, Text, View } from 'react-native'; import { colors, radius, spacing } from '@ui/theme';
export default function MultipleChoiceOptions({ choices, value, onChange }: { choices: string[]; value?: string; onChange: (v: string) => void }) { return <View style={{ gap: spacing.sm }}>{choices.map((choice) => <Pressable key={choice} onPress={() => onChange(choice)} style={{ padding: spacing.md, borderRadius: radius.md, borderWidth: 1, borderColor: value===choice ? colors.primary : colors.border }}><Text style={{ color: colors.text }}>{choice}</Text></Pressable>)}</View>; }
